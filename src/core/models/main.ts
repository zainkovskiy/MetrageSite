export interface IRealtors {
  UID: number;
  avatar: string;
  firstName: string;
  lastName: string;
  officeName: string;
  phone: string;
  speciality: string;
}
export interface INewBuildings {
  UID: number;
  address: string;
  deadLine: string;
  description: string;
  image: string;
  lat: string;
  lng: string;
  name: string;
}
