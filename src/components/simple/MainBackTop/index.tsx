import React from 'react';
import * as S from './style';

const MainBackTop: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  return <S.MainBackTop>{children}</S.MainBackTop>;
};

export default MainBackTop;
